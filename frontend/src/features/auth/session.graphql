query AuthSession {
  session {
    authenticated
    csrfToken
    expiresAt
  }
}

query Session {
  session {
    authenticated
    csrfToken
    expiresAt
    vikunjaUser {
      id
      username
      timezone
      weekStart
      defaultProjectId
    }
  }
}
