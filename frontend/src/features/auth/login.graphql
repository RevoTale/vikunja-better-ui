mutation Login($input: LoginInput!) {
  login(input: $input) {
    session {
      authenticated
      csrfToken
      expiresAt
      vikunjaUser {
        id
        username
        timezone
        weekStart
        defaultProjectId
      }
    }
  }
}
